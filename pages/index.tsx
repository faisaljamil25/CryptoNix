import type { NextPage } from 'next';

const Home: NextPage = () => {
  const title: string = 'sfs';
  console.log(title);

  return (
    <div>
      <h1>CryptoNix</h1>
    </div>
  );
};

export default Home;
